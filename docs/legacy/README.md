# Legacy Docs

This folder stores historical planning, architecture, security, seed, and reference-era files that are no longer the active day-to-day source of truth.

Moved here to keep the root docs folder focused on:

- `docs/roadmap-post-alpha.md`
- `docs/devlog.md`
- active runbooks
- active role/finance references
- module planning docs under `docs/planning/`

Use these files for historical context only. If any legacy file conflicts with current code, migrations, `roadmap-post-alpha.md`, `devlog.md`, or an active runbook, treat the active source as authoritative.
